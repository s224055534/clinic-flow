import { useEffect, useState } from 'react';

// Keep the demonstration fixed to one fictional clinic day
const DEMO_DATE = '2026-09-16';
const emptyPatient = { fullName: '', phone: '', dateOfBirth: '' };
const emptyBooking = { patientId: '', staffId: '', startsAt: `${DEMO_DATE}T09:00`, endsAt: `${DEMO_DATE}T09:30`, reason: '' };

export default function App() {
  // Hold the temporary dashboard data and form values in browser memory
  const [role, setRole] = useState('Receptionist'); const [date, setDate] = useState(DEMO_DATE); const [search, setSearch] = useState('');
  const [data, setData] = useState({ patients: [], staff: [], appointments: [] }); const [patient, setPatient] = useState(emptyPatient); const [booking, setBooking] = useState(emptyBooking);
  const [reschedule, setReschedule] = useState(null); const [message, setMessage] = useState(''); const [refreshKey, setRefreshKey] = useState(0);

  // Simulate the data operations that the API will take over later
  useEffect(() => { async function loadData() { return { patients: [{ id: 1, fullName: 'Maya Chen', phone: '555-0101', dateOfBirth: '1992-04-14' }, { id: 2, fullName: 'Omar Rivera', phone: '555-0102', dateOfBirth: '1985-11-03' }, { id: 3, fullName: 'Priya Nair', phone: '555-0103', dateOfBirth: '2001-07-22' }], staff: [{ id: 1, fullName: 'Dr. Lena Ortiz' }, { id: 2, fullName: 'Dr. Noah Williams' }], appointments: [] }; } loadData().then(setData); }, []);
  async function request(_path, options) { return JSON.parse(options.body); }
  function refresh(successMessage) { setMessage(successMessage); setRefreshKey((value) => value + 1); }
  async function submitPatient(event) { event.preventDefault(); const body = await request('/api/patients', { body: JSON.stringify(patient) }); setData((current) => ({ ...current, patients: [...current.patients, { ...body, id: Date.now() }] })); setPatient(emptyPatient); refresh('Fictional patient added.'); }
  async function submitBooking(event) { event.preventDefault(); const body = await request('/api/appointments', { body: JSON.stringify(booking) }); const selectedPatient = data.patients.find((item) => String(item.id) === body.patientId); const selectedStaff = data.staff.find((item) => String(item.id) === body.staffId); setData((current) => ({ ...current, appointments: [...current.appointments, { ...body, id: Date.now(), patientName: selectedPatient.fullName, phone: selectedPatient.phone, staffName: selectedStaff.fullName, startTime: body.startsAt.slice(11), endTime: body.endsAt.slice(11), status: 'Scheduled' }] })); refresh('Appointment booked.'); }
  async function updateStatus(id, status) { setData((current) => ({ ...current, appointments: current.appointments.map((item) => item.id === id ? { ...item, status } : item) })); refresh(`Appointment marked ${status}.`); }
  async function submitReschedule(event) { event.preventDefault(); setData((current) => ({ ...current, appointments: current.appointments.map((item) => item.id === reschedule.id ? { ...item, ...reschedule, startTime: reschedule.startsAt.slice(11), endTime: reschedule.endsAt.slice(11), staffName: current.staff.find((staff) => String(staff.id) === reschedule.staffId).fullName, status: 'Scheduled' } : item) })); setReschedule(null); refresh('Appointment rescheduled.'); }

  // Derive the visible patient list and daily summary counts
  const patientMatches = data.patients.filter((item) => { const term = search.toLowerCase(); return item.fullName.toLowerCase().includes(term) || item.phone.includes(term); });
  const scheduled = data.appointments.filter((item) => item.status === 'Scheduled').length;
  const waiting = data.appointments.filter((item) => item.status === 'Arrived').length;
  const completed = data.appointments.filter((item) => item.status === 'Completed').length;

  // Render every dashboard control from the temporary state
  return (<main className="shell"><header className="hero"><div><p className="eyebrow">Fictional data only</p><h1>ClinicFlow</h1><p>Daily appointments, patient lookup, and a safer booking workflow.</p></div><label className="role-picker">Demo role<select value={role} onChange={(event) => setRole(event.target.value)}><option>Receptionist</option><option>Clinician</option></select></label></header><p className="notice">This learning prototype is not production authentication and must not contain real patient data.</p>{message && <p className="message" aria-live="polite">{message}</p>}<section className="toolbar card"><label>Schedule date<input type="date" value={date} onChange={(event) => { const nextDate = event.target.value; setDate(nextDate); setBooking((current) => ({ ...current, startsAt: `${nextDate}T09:00`, endsAt: `${nextDate}T09:30` })); }} /></label><label>Search patients or appointments<input type="search" value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Name, phone, clinician, or reason" /></label></section><section className="metrics"><article className="card"><strong>{data.appointments.length}</strong><span>Total today</span></article><article className="card"><strong>{scheduled}</strong><span>Scheduled</span></article><article className="card"><strong>{waiting}</strong><span>Waiting</span></article><article className="card"><strong>{completed}</strong><span>Completed</span></article></section><section className="card"><h2>Daily staff calendar</h2><div className="table-wrap"><table><thead><tr><th>Time</th><th>Patient</th><th>Clinician</th><th>Reason</th><th>Status</th><th>Actions</th></tr></thead><tbody>{data.appointments.filter((item) => item.startsAt.startsWith(date) && `${item.patientName} ${item.phone} ${item.staffName} ${item.reason}`.toLowerCase().includes(search.toLowerCase())).map((appointment) => <tr key={appointment.id}><td>{appointment.startTime} to {appointment.endTime}</td><td>{appointment.patientName}<small>{appointment.phone}</small></td><td>{appointment.staffName}</td><td>{appointment.reason}</td><td><span className={`badge ${appointment.status.toLowerCase().replaceAll(' ', '-')}`}>{appointment.status}</span></td><td className="actions">{role === 'Receptionist' ? <><button onClick={() => updateStatus(appointment.id, 'Arrived')}>Arrived</button><button onClick={() => setReschedule({ id: appointment.id, staffId: String(appointment.staffId), startsAt: appointment.startsAt, endsAt: appointment.endsAt })}>Reschedule</button><button className="danger" onClick={() => updateStatus(appointment.id, 'Cancelled')}>Cancel</button></> : <><button onClick={() => updateStatus(appointment.id, 'In Consultation')}>Start visit</button><button onClick={() => updateStatus(appointment.id, 'Completed')}>Complete</button></>}</td></tr>)}{!data.appointments.length && <tr><td colSpan="6">No matching appointments.</td></tr>}</tbody></table></div></section>{role === 'Receptionist' ? <section className="two-column"><form className="card" onSubmit={submitBooking}><h2>Book appointment</h2><label>Patient<select required value={booking.patientId} onChange={(event) => setBooking({ ...booking, patientId: event.target.value })}><option value="">Choose a patient</option>{data.patients.map((item) => <option key={item.id} value={item.id}>{item.fullName}</option>)}</select></label><label>Clinician<select required value={booking.staffId} onChange={(event) => setBooking({ ...booking, staffId: event.target.value })}><option value="">Choose a clinician</option>{data.staff.map((item) => <option key={item.id} value={item.id}>{item.fullName}</option>)}</select></label><label>Starts<input required type="datetime-local" value={booking.startsAt} onChange={(event) => setBooking({ ...booking, startsAt: event.target.value })} /></label><label>Ends<input required type="datetime-local" value={booking.endsAt} onChange={(event) => setBooking({ ...booking, endsAt: event.target.value })} /></label><label>Reason<input required value={booking.reason} onChange={(event) => setBooking({ ...booking, reason: event.target.value })} /></label><button className="primary" type="submit">Confirm booking</button></form><form className="card" onSubmit={submitPatient}><h2>Add fictional patient</h2><label>Full name<input required value={patient.fullName} onChange={(event) => setPatient({ ...patient, fullName: event.target.value })} /></label><label>Phone<input required value={patient.phone} onChange={(event) => setPatient({ ...patient, phone: event.target.value })} /></label><label>Date of birth<input required type="date" value={patient.dateOfBirth} onChange={(event) => setPatient({ ...patient, dateOfBirth: event.target.value })} /></label><button className="primary" type="submit">Add patient</button></form></section> : <section className="card"><p>Clinicians can update clinical statuses but cannot create patients, book, reschedule, or cancel appointments.</p></section>}{role === 'Receptionist' && reschedule && <form className="card" onSubmit={submitReschedule}><h2>Reschedule appointment #{reschedule.id}</h2><div className="form-grid"><label>Clinician<select required value={reschedule.staffId} onChange={(event) => setReschedule({ ...reschedule, staffId: event.target.value })}>{data.staff.map((item) => <option key={item.id} value={item.id}>{item.fullName}</option>)}</select></label><label>Starts<input required type="datetime-local" value={reschedule.startsAt} onChange={(event) => setReschedule({ ...reschedule, startsAt: event.target.value })} /></label><label>Ends<input required type="datetime-local" value={reschedule.endsAt} onChange={(event) => setReschedule({ ...reschedule, endsAt: event.target.value })} /></label></div><div className="actions"><button className="primary" type="submit">Save new time</button><button type="button" onClick={() => setReschedule(null)}>Close</button></div></form>}<section className="card"><h2>Patient records</h2><div className="patient-list">{patientMatches.map((item) => <article key={item.id}><strong>{item.fullName}</strong><span>{item.phone}</span><span>DOB: {item.dateOfBirth}</span></article>)}{!patientMatches.length && <p>No matching fictional patients.</p>}</div></section></main>); }